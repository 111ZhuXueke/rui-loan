<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.rui.control.mapper.IUserMapper" >
  
    <!-- 公共查询语句 -->
	<sql id="selectSql">
		select 
		    <trim suffixOverrides="," >
		    	u.id as userId,
				u.userName as userName,
				u.phone as phone,
				u.password as password,
				u.epassword as epassword
		    </trim>
	   	from
	    	`users` as u
	</sql>
	<select id="getAll" resultType="com.rui.control.domain.UserDomain">
		  SELECT
		  	u.id as userId,
			u.userName as userName,
			u.phone as phone,
			u.password as password,
			u.epassword as epassword
		  FROM user u
	</select>
</mapper>